# Copy to .env locally; source it before invoking generation scripts.
# Replace the empty key locally. Never commit your .env.
export OPENAI_API_KEY=""
export OPENAI_BASE_URL="https://api.openai.com/v1"
# Set to 1 only when your model rejects the temperature parameter.
export STRUCTEVAL_OMIT_TEMPERATURE="0"
